<div class="container">
  <h2>Users</h2>

  <table class="table table-striped">
  <tr>
    <th>First name</th>
    <th>Last name</th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.first_name %></td>
    <td><%= user.last_name %></td>
  </tr>
<% end %>
</table>

</div>
